name: API CI/CD

on:
  pull_request:
    paths:
      - "api/**"
      - ".github/workflows/api-ci-cd.yml"
  push:
    branches: [main]
    paths:
      - "api/**"
      - ".github/workflows/api-ci-cd.yml"

defaults:
  run:
    working-directory: api

env:
  ACR_NAME: acrreceptenappdev
  IMAGE_NAME: receptenapp-api
  WEBAPP_NAME: receptenapp-api-dev
  RESOURCE_GROUP: kladje-dev

jobs:
  lint-test-build:
    name: Lint, typecheck, test, build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: receptenapp
          POSTGRES_PASSWORD: receptenapp
          POSTGRES_DB: receptenapp_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      # Tests hit a real, ephemeral Postgres — never a paid API, so this is
      # fine in CI. Clerk isn't wired to a real instance yet (task 0.9's
      # credential ask); these are just enough for Settings to validate and
      # for the readyz test to see a JWKS fetch fail cleanly.
      DATABASE_URL: postgresql+asyncpg://receptenapp:receptenapp@localhost:5432/receptenapp_test
      CLERK_SECRET_KEY: sk_test_ci_placeholder
      CLERK_JWKS_URL: https://example.invalid/.well-known/jwks.json
      CLERK_WEBHOOK_SECRET: whsec_ci_placeholder
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - run: uv sync
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy src/receptenapp
      - run: uv run alembic upgrade head
      - run: uv run pytest -q

      - name: Build image (no push on PRs)
        run: docker build -t "${IMAGE_NAME}:ci" .

  deploy:
    name: Push to ACR and deploy
    needs: lint-test-build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Log in to ACR
        run: az acr login --name "${{ env.ACR_NAME }}"

      - name: Build and push
        run: |
          IMAGE="${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}"
          docker build -t "$IMAGE:${{ github.sha }}" -t "$IMAGE:latest" .
          docker push "$IMAGE:${{ github.sha }}"
          docker push "$IMAGE:latest"

      # dev runs on a Basic (B1) plan, which does not support deployment
      # slots at all — this deploys directly. Slot-swap is for kladje-prod
      # once it's on a Standard/Premium plan (see 01-architecture.md).
      - name: Deploy to App Service
        run: |
          az webapp config container set \
            --name "${{ env.WEBAPP_NAME }}" \
            --resource-group "${{ env.RESOURCE_GROUP }}" \
            --container-image-name "${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          az webapp restart --name "${{ env.WEBAPP_NAME }}" --resource-group "${{ env.RESOURCE_GROUP }}"
